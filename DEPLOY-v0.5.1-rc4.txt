RBZ PC Health v0.5.1 RC4

Replace:
- Modules\Service.psm1
- RBZHealth.ps1
- Modules\Report.psm1
- Config\settings.json
- README.md

Then:

git status
git diff

git add .
git commit -m "RBZ PC Health v0.5.1 remove restore command precheck"
git push origin main

Test locally:
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\RBZHealth.ps1

Expected on the current test PC:
1. Run Full Scan.
2. Select DISM RestoreHealth.
3. RBZ attempts Checkpoint-Computer directly.
4. A new RBZ PC Health pre-repair restore point should be created and verified.
5. No false "Checkpoint-Computer was not found" prompt should appear.
6. DISM then starts.

Verify:
Get-ComputerRestorePoint |
  Sort-Object SequenceNumber -Descending |
  Select-Object -First 5 SequenceNumber,Description,CreationTime

Then repeat with SFC /scannow.

If all good:
.\build-release.ps1

gh release create v0.5.1 `
  ".\dist\RBZ-PC-Health-0.5.1.zip" `
  ".\dist\RBZ-PC-Health-0.5.1.sha256" `
  --repo rbznet/RBZTechSolutions `
  --title "RBZ PC Health v0.5.1" `
  --notes "Add repair progress, verification, and verified restore-point protection."
