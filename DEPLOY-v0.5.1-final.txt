RBZ PC Health v0.5.1 FINAL patch

Replace:
- Config\settings.json
- Modules\Service.psm1
- Modules\Report.psm1
- RBZHealth.ps1
- README.md

Then:

git status
git diff

git add .
git commit -m "RBZ PC Health v0.5.1 final repair protection workflow"
git push origin main

Local test:
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\RBZHealth.ps1

Test on a machine with System Protection DISABLED:
1. Run Full Scan.
2. Select DISM RestoreHealth.
3. Confirm prompt offers Enable / Continue Without Restore Point / Cancel.
4. Test Cancel first.
5. Test Continue Without Restore Point and confirm second warning.
6. Test Enable System Protection.
7. Confirm restore point creation is VERIFIED before repair starts.
8. Run SFC /scannow and confirm the same protection logic.
9. Verify Before / After and reports.

When fully tested:
.\build-release.ps1

gh release create v0.5.1 `
  ".\dist\RBZ-PC-Health-0.5.1.zip" `
  ".\dist\RBZ-PC-Health-0.5.1.sha256" `
  --repo rbznet/RBZTechSolutions `
  --title "RBZ PC Health v0.5.1" `
  --notes "Add live repair progress, verification, and explicit System Protection preflight."

Final bootstrap:
irm https://raw.githubusercontent.com/rbznet/RBZTechSolutions/main/bootstrap.ps1 | iex
