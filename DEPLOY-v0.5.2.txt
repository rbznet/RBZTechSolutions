RBZ PC Health v0.5.2

Replace:
- Config\settings.json
- RBZHealth.ps1
- README.md

Keep all tested v0.5.1 modules unchanged.

Git:
git status
git diff
git add .
git commit -m "RBZ PC Health v0.5.2 add full scan progress"
git push origin main

LOCAL TEST FROM WINDOWS TERMINAL / POWERSHELL 7:
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\RBZHealth.ps1

Test:
1. Run Full Scan.
2. Confirm progress moves through all 8 modules.
3. Confirm module count, percentage and elapsed time update.
4. Confirm UI remains responsive.
5. Confirm Reports and Repair Centre re-enable after completion.
6. Run a second scan and confirm Before / After still works.
7. Test one Repair Centre action to ensure v0.5.1 behaviour is unchanged.

Bootstrap:
irm https://raw.githubusercontent.com/rbznet/RBZTechSolutions/main/bootstrap.ps1 | iex

When tested:
.\build-release.ps1

gh release create v0.5.2 `
  ".\dist\RBZ-PC-Health-0.5.2.zip" `
  ".\dist\RBZ-PC-Health-0.5.2.sha256" `
  --repo rbznet/RBZTechSolutions `
  --title "RBZ PC Health v0.5.2" `
  --notes "Add responsive module-by-module Full Scan progress and elapsed time."
