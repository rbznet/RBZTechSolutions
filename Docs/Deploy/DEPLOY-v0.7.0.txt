RBZ PC Health v0.7.0 RC1

ADD:
- Modules\EventLog.psm1
- Assets\rbz-logo.png
- Docs\Deploy\DEPLOY-v0.7.0.txt

REPLACE:
- Config\settings.json
- RBZHealth.ps1
- README.md

RUN FROM WINDOWS TERMINAL:
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\RBZHealth.ps1

TEST:
1. Confirm logo appears in header.
2. Run Full Scan.
3. Confirm progress reaches Module 9 of 9.
4. Confirm new findings:
   - Unexpected shutdowns
   - Crash / BSOD history
   - WHEA hardware errors
   - Storage event log health
   - Application crash history
5. Inspect Details for any non-healthy result.
6. Confirm isolated application errors remain Info until thresholds are reached.
7. Generate Customer and Technician reports and check the new findings appear.
8. Confirm Repair Centre, Before/After, Dark mode and responsive scrolling are unchanged.
9. Test on desktop and laptop before committing.

WHEN APPROVED:
git status
git diff
git add -A
git commit -m "RBZ PC Health v0.7.0 add event log and crash diagnostics"
git push origin main


RC2 TEST:
1. Re-run Full Scan on the desktop used for RC1 validation.
2. Storage event log health should no longer count healthy NTFS 98 events.
3. If no other genuine storage events exist, Storage event log health should become Healthy.
4. Application crash history should show a grouped summary by executable.
5. Confirm repeated DayZ/tbs_browser failures still escalate appropriately.
6. Inspect Details for grouped module/exception information.
7. Test on the laptop and compare classifications.


RC3 TEST:
1. Run Full Scan.
2. Open Application crash history.
3. Confirm DayZ-style entries with module=unknown and exception=0x00000000 show only the executable/event count in Grouped applications.
4. Confirm useful values such as qbcore.dll / 0x80000003 remain visible.
5. Raw recent event examples should remain unchanged.
6. Recheck Storage event log health and confirm benign healthy NTFS 98 events remain excluded.
