RBZ PC Health v0.8.0 RC10b

PATCHER FIX

RC10a used a quote-specific XAML validation fragment:
Name='RestartRequiredBanner'

The committed RBZHealth.ps1 XAML uses:
Name="RestartRequiredBanner"

The validation therefore stopped before modifying RBZHealth.ps1.

RC10b uses broad capability checks and does not depend on XAML quote style
or historical internal comment markers.

The failed RC10 and RC10a attempts did not modify RBZHealth.ps1 because their
validation guards execute before any file replacement/write operation.

APPLY

powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Tools\Patches\Apply-RC10.ps1

EXPECTED

RBZ PC Health v0.8.0 RC10b applied.
Persistent post-restart repair verification has been added.
RBZHealth.ps1 passed the PowerShell parser check.

Do not proceed to the reboot test until the patch reports successful application.
