RBZ PC Health v0.2.5 update

Replace:
- Config\settings.json
- Modules\System.psm1
- README.md

This assumes the current v0.2.4/v0.2.3 codebase is already installed.

Then:

git status
git diff

git add .
git commit -m "RBZ PC Health v0.2.5 Windows Time accuracy fix"
git push

Test locally:
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\RBZHealth.ps1

On the laptop described during testing, expected Windows Time result:

Status: Info
Summary: Windows Time is configured for synchronization; the service is currently stopped.

If correct:
.\build-release.ps1

gh release create v0.2.5 `
  ".\dist\RBZ-PC-Health-0.2.5.zip" `
  ".\dist\RBZ-PC-Health-0.2.5.sha256" `
  --repo rbznet/RBZTechSolutions `
  --title "RBZ PC Health v0.2.5" `
  --notes "Improve Windows Time diagnostics and remove stopped-service false positives."

Final bootstrap test:
irm https://raw.githubusercontent.com/rbznet/RBZTechSolutions/main/bootstrap.ps1 | iex
