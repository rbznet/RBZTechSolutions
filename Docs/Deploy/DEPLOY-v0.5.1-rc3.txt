RBZ PC Health v0.5.1 RC3

Replace:
- Config\settings.json
- Modules\Service.psm1
- RBZHealth.ps1
- Modules\Report.psm1
- README.md

Then:

git status
git diff

git add .
git commit -m "RBZ PC Health v0.5.1 fix restore point verification logic"
git push origin main

Test:
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\RBZHealth.ps1

Expected on the test PC where manual Checkpoint-Computer works:
1. Select DISM RestoreHealth.
2. RBZ creates a new "RBZ PC Health pre-repair" restore point.
3. RBZ verifies the new sequence number.
4. No System Protection prompt should appear.
5. DISM then starts.

Confirm with:
Get-ComputerRestorePoint |
  Sort-Object SequenceNumber -Descending |
  Select-Object -First 5 SequenceNumber,Description,CreationTime

Then repeat with SFC /scannow.

If good:
.\build-release.ps1

gh release create v0.5.1 `
  ".\dist\RBZ-PC-Health-0.5.1.zip" `
  ".\dist\RBZ-PC-Health-0.5.1.sha256" `
  --repo rbznet/RBZTechSolutions `
  --title "RBZ PC Health v0.5.1" `
  --notes "Add repair progress, verification, and verified restore-point protection."
