RBZ PC Health v0.8.0 RC11
Safe RC bootstrap TEMP self-cleanup

BASELINE
GitHub main commit:
604ae3543227da3e431e1098c38585cc9427f353
v0.8.0 RC10c add persistent post-restart repair verification

CHANGES
- Unique session path: %TEMP%\RBZ-PC-Health\RC\<GUID>\
- Removes only the exact session folder after RBZ exits.
- Uses try/finally so normal exits and script-error exits still attempt cleanup.
- Prunes abandoned marked sessions older than 24 hours.
- Preserves generated reports under Documents\RBZ PC Health Reports\RC.
- Leaves ProgramData repair-state files untouched.
- Sets bootstrap.cleanupAfterExit=true.
- Stable bootstrap.ps1 is unchanged in RC11.

APPLY
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Tools\Patches\Apply-RC11.ps1

TEST
Run the LOCAL bootstrap file:
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\bootstrap-rc.ps1

While RBZ is open, in a second PowerShell:
Get-ChildItem "$env:TEMP\RBZ-PC-Health\RC" -Force

Expected: one GUID-named session folder.

Optional report test:
- Run Full Scan.
- Generate Customer or Technician report.
- Close RBZ.

After RBZ closes:
Test-Path "$env:TEMP\RBZ-PC-Health\RC"
Get-ChildItem ([IO.Path]::Combine([Environment]::GetFolderPath('MyDocuments'),'RBZ PC Health Reports\RC')) -File -Recurse

Expected:
- GUID session folder is gone.
- RC parent may be gone if empty.
- Reports remain in Documents\RBZ PC Health Reports\RC.

Do not commit until this lifecycle test passes.
