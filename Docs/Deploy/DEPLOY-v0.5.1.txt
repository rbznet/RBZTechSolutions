RBZ PC Health v0.5.1 update

Replace:
- Config\settings.json
- Modules\Service.psm1
- Modules\Report.psm1
- RBZHealth.ps1
- README.md

Then:

git status
git diff

git add .
git commit -m "RBZ PC Health v0.5.1 repair progress and verification"
git push

TEST LOCALLY:
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\RBZHealth.ps1

Recommended first test:
1. Run Full Scan.
2. Select ONLY DISM RestoreHealth.
3. Confirm the UI stays responsive and elapsed time updates.
4. Confirm live progress/activity is shown.
5. Confirm Windows Component Store appears as Verified.
6. Select ONLY SFC /scannow.
7. Confirm Protected System Files appears as Verified.
8. Run Full Scan again.
9. Generate both report types.

If good:

.\build-release.ps1

gh release create v0.5.1 `
  ".\dist\RBZ-PC-Health-0.5.1.zip" `
  ".\dist\RBZ-PC-Health-0.5.1.sha256" `
  --repo rbznet/RBZTechSolutions `
  --title "RBZ PC Health v0.5.1" `
  --notes "Add live repair progress and DISM/SFC verification integration."

Final:
irm https://raw.githubusercontent.com/rbznet/RBZTechSolutions/main/bootstrap.ps1 | iex
