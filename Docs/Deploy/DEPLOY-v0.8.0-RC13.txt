RBZ PC Health v0.8.0 RC13
Stable bootstrap lifecycle hardening

BASELINE
aaecc9611a76b4c623953ce7a9037f36387d3902
v0.8.0 RC12 add boot-aware repair verification

RC13 ports the proven RC TEMP lifecycle to stable bootstrap.ps1 while keeping
stable release-based and SHA256 verified.

- Unique %TEMP%\RBZ-PC-Health\Stable\<GUID> sessions
- Narrow .rbz-session marker
- Marked abandoned sessions older than 24h are eligible for cleanup
- Generated reports preserved to Documents\RBZ PC Health Reports\Stable
- Exact session removed after RBZ exits when cleanupAfterExit=true
- ProgramData repair state is untouched
- bootstrap-rc.ps1 is untouched

APPLY
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Tools\Patches\Apply-RC13.ps1

TEST LOCALLY BEFORE COMMIT
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\bootstrap.ps1

NOTE: stable bootstrap intentionally downloads the CURRENT latest published
release, not GitHub main.

While RBZ is open:
Get-ChildItem "$env:TEMP\RBZ-PC-Health\Stable" -Force
Expected: one GUID folder.

Generate Customer or Technician report, close RBZ, then:
Test-Path "$env:TEMP\RBZ-PC-Health\Stable"
Expected: False

Get-ChildItem "$([Environment]::GetFolderPath('MyDocuments'))\RBZ PC Health Reports\Stable" -File -Recurse |
 Select-Object Name,Length,LastWriteTime,FullName
Expected: generated report HTML/JSON files.

Do not commit until these tests pass.
