RBZ PC Health v0.5.1 RC5

Replace:
- Config\settings.json
- Modules\Service.psm1
- RBZHealth.ps1
- Modules\Report.psm1
- README.md

Git:
git status
git diff
git add .
git commit -m "RBZ PC Health v0.5.1 isolate restore point creation"
git push origin main

Local test:
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\RBZHealth.ps1

Expected on the current test PC:
1. Select DISM RestoreHealth.
2. RBZ launches a clean Windows PowerShell child process for Checkpoint-Computer.
3. A new RBZ restore point is created.
4. RBZ verifies the new sequence number.
5. DISM starts without the module/autoload error.

Verify:
Get-ComputerRestorePoint |
  Sort-Object SequenceNumber -Descending |
  Select-Object -First 5 SequenceNumber,Description,CreationTime

Then test SFC /scannow.

If good:
.\build-release.ps1

gh release create v0.5.1 `
  ".\dist\RBZ-PC-Health-0.5.1.zip" `
  ".\dist\RBZ-PC-Health-0.5.1.sha256" `
  --repo rbznet/RBZTechSolutions `
  --title "RBZ PC Health v0.5.1" `
  --notes "Add live repair progress, verification, and isolated restore-point protection."
